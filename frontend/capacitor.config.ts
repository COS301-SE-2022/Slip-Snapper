import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'slip-snapper',
  webDir: 'dist/apps/slip-snapper',
  bundledWebRuntime: false,
};

export default config;
